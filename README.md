# CineplayTv (Vite + React)

Projeto pronto para rodar localmente (VS Code) e publicar na Vercel.

Instalação local:

1. npm install
2. Copie .env.example para .env e adicione sua chave TMDB: VITE_TMDB_API_KEY=SUA_CHAVE_AQUI
3. npm run dev
